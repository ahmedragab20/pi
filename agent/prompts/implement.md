---
description: Inspect, plan when substantial, implement, and verify a scoped change
argument-hint: "<query>"
---
Implement: $@.

Inspect the relevant code and current diff first. Make the smallest scoped change and
run proportionate checks. For substantial work, give a short plan before editing. Ask
for approval when the change is materially ambiguous or risky; do not impose a web gate
on every edit. Keep unrelated user edits intact. The lead owns implementation and
review; use the optional worker only for a substantial bounded independent chore.
