---
description: Lead verification pass over current changes
---
Verify the current changes yourself. Read the real scoped diff and every changed file,
grade it against the request, and inspect for out-of-scope edits without reverting
unrelated user work. Run proportionate checks, including `npm run check --prefix
agent/npm` when relevant, and report actual output. State honest unverified gaps; never
claim a pass from a worker report alone.
