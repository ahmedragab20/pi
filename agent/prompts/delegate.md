---
description: Delegate one bounded independent chore to the optional worker and review its result
argument-hint: "<what to delegate>"
---
Delegate only a substantial, independent chore: $@.

Use the single optional worker at most when it is genuinely bounded. Give it a goal,
exact scope, inputs, checks, bounded turns, and explicit out-of-scope items. It must not
spawn another worker. Review the actual result and diff yourself; never treat its report
as evidence. The lead owns decisions, integration, fixes, and final verification.
